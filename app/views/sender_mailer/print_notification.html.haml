- if @letter.recipients.size == 1
  = "You have sent a letter to: #{@letter.recipients.first.legislator.name}"
  = "Your letter should arrive in 5 business days."
  = "You will be notified when it arrives."
- else
  = "You have sent letters to: #{@letter.recipients.map { |recipient| recipient.legislator.name }.join(', ') }}"
  = "Your letters should arrive in 5 business days."
  = "You will be notified when they arrive."
