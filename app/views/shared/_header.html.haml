#menus.group
  - if advocacy_group_signed_in?
    = render :partial => 'shared/signed_in_menu'
  - else
    = render :partial => 'shared/signed_out_menu'

#header
  = link_to 'MailCongress', root_url, :class => 'logo'
