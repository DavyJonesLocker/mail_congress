- unless Rails.env == 'test' || Rails.env == 'cucumber'
  - content_for :javascript do
    = javascript_include_tag 'highcharts', 'campaign'

.article
  %h1= @campaign.title
  .summary
    = @campaign.summary
  .body
    = @campaign.body
  .more
    = link_to '← more campaigns', '/dashboard'
  
.aside  
  %ul.actions
    %li= link_to 'View', campaign_permalink_url(@campaign, :protocol => 'https')
    %li= link_to 'Edit', [:edit, @campaign]

  #chart